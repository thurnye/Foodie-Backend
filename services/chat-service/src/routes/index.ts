/**
 * REST API Routes for Chat Service
 * TODO: Phase 2 - Implement REST fallback endpoints
 *
 * Routes to implement:
 * - GET /api/chats/:groupId - Get chat history
 * - POST /api/chats/:groupId/message - Send message (REST fallback)
 * - GET /api/chats/user/:userId - Get user's chat rooms
 * - DELETE /api/chats/message/:messageId - Delete message
 * - PUT /api/chats/message/:messageId - Edit message
 * - GET /api/chats/unread - Get unread message count
 */

import { Router } from 'express';

const router = Router();

// TODO: Implement REST routes

export default router;
